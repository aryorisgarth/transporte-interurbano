# URL del backend (vacío = mismo host con proxy Vite en dev)
VITE_API_URL=

# Realm Keycloak (sin /protocol/...)
VITE_KEYCLOAK_URL=http://localhost:8180/realms/transporte-bluefields

# Modo demo estático para Vercel (sin backend ni Keycloak)
# VITE_USE_MOCK=true
